# UDP Knock 项目需求规范说明书

## 1. 项目概述

### 1.1 项目名称
UDP Knock - 安全的远程防火墙管理工具

### 1.2 项目目标
开发一个基于 Rust 语言的安全 UDP 远程防火墙管理工具，支持 OpenWrt 平台（aarch64_cortex-a53 和 x86_64），并具备扩展到 Linux 和 Windows 平台的能力。

### 1.3 核心功能
- 通过 UDP 协议远程管理防火墙规则
- 提供端到端加密通信
- 支持防火墙规则的增删改查和启用/禁用

---

## 2. 功能需求

### 2.1 服务端功能
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 命令接收 | 接收并解析客户端发送的命令 | P0 |
| 命令执行 | 执行防火墙相关命令（启用/禁用/新建规则等） | P0 |
| 结果返回 | 将执行结果加密返回给客户端 | P0 |
| 静默拒绝 | 非法请求静默不应答（调试模式除外） | P1 |

### 2.2 客户端功能
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 命令发送 | 向服务端发送加密命令 | P0 |
| 响应接收 | 接收并解密服务端响应 | P0 |

### 2.3 防火墙管理命令
| 命令 | 参数 | 说明 |
|------|------|------|
| `list` | `[过滤关键字]` | 列出所有防火墙规则 |
| `enable` | `<规则名> [in\|out]` | 启用指定规则（可指定方向，默认 in） |
| `disable` | `<规则名> [in\|out]` | 禁用指定规则（可指定方向，默认 in） |
| `create` | `<名称> <源> <目标> <协议> <端口> [in\|out]` | 新建规则（可指定方向，默认 in） |
| `delete` | `<规则名>` | 删除规则 |
| `status` | 无 | 获取防火墙状态 |

---

## 3. 安全需求

### 3.1 加密方案
| 特性 | 实现方式 |
|------|----------|
| 加密算法 | AES-256-GCM |
| 防篡改 | HMAC-SHA256 |
| 防重放 | 时间戳验证（30秒窗口） |
| 密钥派生 | SHA256（从共享密钥派生） |
| 数据编码 | Base64 |

### 3.2 安全策略
- 非法请求静默丢弃（不返回任何响应）
- 调试模式下可记录日志
- 支持客户端 IP 白名单

### 3.3 通信流程
```
客户端                          服务端
   |                              |
   |--- JSON命令 + 时间戳 ------->|
   |    AES-256-GCM 加密          |
   |                              | 验证HMAC
   |                              | 验证时间戳
   |                              | 执行命令
   |<--- JSON响应 ----------------|
   |     AES-256-GCM 加密         |
```

---

## 4. 技术需求

### 4.1 编程语言与版本
- **语言**: Rust (Edition 2021)
- **工具链**: Stable (1.75+)


### 4.2 平台支持
| 平台 | 架构 | 状态 |
|------|------|------|
| OpenWrt | aarch64_cortex-a53 (ARMv8 64位) | ✅ 完整实现 |
| OpenWrt | x86_64 | ✅ 完整实现 |
| Linux | x86_64 | 🟡 占位实现 |
| Windows | x86_64 | 🟡 占位实现 |
注意openwrt 需要区分高低版本下不同防火墙软件iptables/fw4支持
---


## 7. CI/CD 需求

### 7.1 构建GitHub Action平台
| 平台 | Target | Features |
|------|--------|----------|
| Windows | `x86_64-pc-windows-msvc` | `windows` |
| Linux Ubuntu | `x86_64-unknown-linux-gnu` | `linux` |
| OpenWrt aarch64 | `aarch64-unknown-linux-gnu` | `openwrt` |
| OpenWrt x86_64 | `x86_64-unknown-linux-gnu` | `openwrt` |


---

## 8. 命令行接口

### 8.1 命令格式
```bash
udp-knock [OPTIONS] <COMMAND>
```

### 8.2 全局选项
| 选项 | 说明 |
|------|------|
| `-c, --config <FILE>` | 配置文件路径（默认: config.json） |
| `-d, --debug` | 启用调试模式 |
| `-p, --platform <PLATFORM>` | 指定平台 |

### 8.3 子命令
| 命令 | 说明 | 示例 |
|------|------|------|
| `server` | 启动服务端 | `udp-knock server` |
| `client` | 发送客户端命令 | `udp-knock client --action list` |
| `init` | 生成配置文件 | `udp-knock init --platform openwrt` |
| `status` | 显示防火墙状态 | `udp-knock status` |

### 8.4 Client 参数
| 参数 | 说明 |
|------|------|
| `-a, --action <ACTION>` | 命令名称 (list/enable/disable/create/delete/status) |
| `-p, --params <PARAMS>` | 命令参数列表 |
| `-n, --offset <OFFSET>` | 分页索引起始偏移量（默认 0） |
| `-t, --timeout <SECONDS>` | 超时时间（秒） |

---

## 9. 错误处理策略

### 9.1 服务端
| 场景 | 处理方式 |
|------|----------|
| 解密失败 | 静默丢弃，不响应 |
| HMAC验证失败 | 静默丢弃，不响应 |
| 时间戳超时 | 静默丢弃，不响应 |
| 未授权客户端 | 静默丢弃，不响应 |
| 命令解析失败 | 返回错误响应（加密） |
| 防火墙命令失败 | 返回错误响应（加密） |

### 9.2 客户端
| 场景 | 处理方式 |
|------|----------|
| 发送超时 | 返回错误信息 |
| 响应超时 | 返回错误信息 |
| 解密失败 | 返回错误信息 |
| 响应解析失败 | 返回错误信息 |

---

## 10. 性能需求

| 指标 | 要求 |
|------|------|
| 最大数据包大小 | 4096 字节 |
| 命令超时 | 5 秒（可配置） |
| 速率限制 | 60 请求/分钟（可配置） |
| 内存占用 | < 10MB |
| 响应时间 | < 100ms（本地） |

---

## 11. 扩展性设计

### 11.1 防火墙适配层
通过 `FirewallManager` trait 实现多平台扩展：


### 11.2 新增平台步骤
1. 在 `src/firewall/` 创建新平台模块
2. 实现 `FirewallManager` trait
3. 在 `mod.rs` 中添加条件编译
4. 在 `Cargo.toml` 中添加 feature

---

